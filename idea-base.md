# Taco Defender — Idea Base

> Definición viva del juego. Actualizar con `/doc` al cerrar cada tarea.

---

## Concepto

**Género:** Tower Defense hipercasual
**Plataforma:** iOS 14+ / Android API 24+
**Control:** Tap (construir/seleccionar torre) + Drag vertical (paneo de cámara sobre el camino)
**Sesión target:** 3–5 minutos (GDD)
**Propuesta de valor:** Defender la barra de una taquería contra 10 oleadas de plagas hambrientas colocando torres de ingredientes (salsa, hielo, guacamole) sobre un camino serpenteante fijo.

---

## Mecánicas Core

- **Victoria:** Sobrevivir las 10 oleadas del GDD (sección 4).
- **Derrota:** La vida de la taquería (base = 3, mejorable con "Barra Blindada") llega a 0. Cada enemigo que llega al final del camino resta `Constants.BASE_DAMAGE_PER_LEAK` (1) de vida — el GDD no especifica el daño por enemigo, se asumió 1 por igual sin importar el tipo (ver "Asunciones documentadas" abajo).
- **Controles:**
  - Tap sobre ícono de torre (barra inferior) → modo construcción → tap sobre casilla libre → coloca la torre (si alcanza el oro).
  - Tap sobre torre construida → panel de selección (daño, rango, nivel, botones Mejorar/Vender).
  - Drag vertical sobre el tablero → paneo de cámara (histórico — el tablero de 10 filas ya entra completo en la ventana visible entre el HUD y la barra inferior, ver sección "Pendientes"/"Camino en zigzag" para el porqué del cambio; el control queda como no-op inofensivo).

---

## Arquitectura (desviaciones del template genérico)

El template `/new-game` está diseñado sobre el molde de un juego de movimiento/disparo (tipo GuacBlaster Survivor: Player, ProjectileSpawner, PowerUpDropper, GemSpawner, XP/level-up). Taco Defender es un tower defense — no hay avatar de jugador, no hay drag-to-move, no hay recolección de gemas ni power-ups temporales in-run. Adaptaciones documentadas:

- **`src/features/board/`** (nuevo, no está en la lista genérica del skill): grilla + camino + colocación/mejora/venta de torres + TODO el input de mundo (tap/drag). Grid y torres se tratan como una sola feature — colocar una torre depende inherentemente de la celda de grilla; la comunicación INTERNA a esta feature es directa, no por EventBus (ver nota de arquitectura en `EventBus.gd`).
- **`src/features/towers/`** en vez de `player/`: `TowerBase.gd` (sin class_name, subtipos por ruta — regla CLAUDE.md #13) + 3 subtipos delgados que leen `Constants.TOWER_CATALOG` (única fuente de verdad de stats por tipo).
- **`src/features/projectiles/Projectile.gd`**: proyectil sin física (ni Area2D ni CharacterBody2D) — homing hacia una referencia viva, revalida `is_instance_valid()` cada frame (regla CLAUDE.md #44/#59).
- **`src/features/enemies/`**: `EnemyBase.gd` + 3 subtipos (básico/rápido/tank) + `EnemySpawner.gd` (orquesta las 10 oleadas) + `wave_queue.gd` (lógica pura de cola de spawn).
- **Sin capas de física** (`[layer_names]` no existe en project.godot a propósito): targeting de torres y daño en área usan grupos (`&"enemies"`) + distancia, no Area2D/collision_mask. Ver nota extensa en `Constants.gd`.
- **`src/features/meta/upgrade_shop.gd`** + **`src/core/MetaManager.gd`**: mismo patrón que otros proyectos GuacamoleBit (SaveManager ya cubre settings/tutorial; MetaManager, en su propio `user://meta.json`, cubre propinas + las 5 mejoras permanentes — evita acercarse al límite de 20 métodos públicos por clase que exige gdlint, regla CLAUDE.md #52).
- **FTUE ligero** (`HUD.gd::_build_ftue_overlay`): 3 mensajes estáticos con botón "Siguiente"/"Entendido", NO una escena `TutorialGame.tscn` separada — las mecánicas nuevas de este juego (construir/iniciar oleada/mejorar-vender) no encajan con la arquitectura de tutorial del template (pensada para Player/GemSpawner/PowerUpDropper, que no existen acá). `SaveManager.set_tutorial_shown(true)` se llama solo al completar el 3er mensaje, nunca antes.
- **Multi-idioma agregado** (`/mobile-i18n`): es, en, pt_BR, fr. `LocalizationManager.gd` (autoload, después de SaveManager) parsea `assets/translations/translations.txt` (CSV con extensión .txt — bug de Godot #38957) en runtime. `LanguageSelectScreen.tscn` en el primer arranque (`SaveManager.get_language() == ""`), selector de cambio en `SettingsScreen`. Texto estático usa la KEY cruda (auto_translate_mode nativo de Control); texto con valores incrustados usa `tr(&"KEY") % args` + un signal `EventBus.language_changed` para refrescar pantallas vivas (MainMenu/UpgradeScreen) cuando el idioma cambia con ellas abiertas.
- **Sin ilusión de profundidad** (agente `game-feel` §6): un tower defense de grilla top-down 2D no la necesita — el camino es fijo y visible, no hay sensación de "acercarse a cámara" como en un runner.

---

## Asunciones documentadas (GDD ambiguo o incompleto en estos puntos)

1. **Daño de un enemigo que llega a la base:** el GDD no lo especifica. Se asumió 1 vida por enemigo, sin importar el tipo (`Constants.BASE_DAMAGE_PER_LEAK`). Con solo 3-8 vidas totales, dejar pasar unos pocos enemigos es letal — encaja con la dificultad creciente de las oleadas tardías (ej. Oleada 9 "Enjambre" con 35 enemigos).
2. **Orden de spawn dentro de una oleada mixta:** el GDD no dice si los grupos de una oleada se intercalan o van secuenciales. Se implementó secuencial (agota un grupo, luego el siguiente) — determinístico y fácil de testear (`wave_queue.gd::build_spawn_queue()`).
3. **Barra Blindada, nivel 5:** el GDD lista literalmente "3 → 4 → 5 → 6 → 7" (5 valores = base + 4 pasos de +1). Como el sistema de tienda es uniforme (las 5 mejoras usan 5 niveles con el mismo costo por nivel: 100/250/500/1000/2000), se extrapoló el nivel 5 continuando el mismo patrón (+1/nivel) → 8 vidas. Ver `Constants.META_BASE_HP_PER_LEVEL`.
4. **"Clientes Generosos" (+10% propinas):** el GDD no aclara "por nivel" como las otras 4 mejoras, pero la sección dice "cada mejora tiene 5 niveles" de forma general — se asumió también por nivel (nivel 5 = +50% propinas).
5. **Forma del camino:** el GDD no define su trazado exacto, solo que es "fijo" (dentro de una misma partida — eso no cambió). Se diseñaron 10 serpenteos distintos de 6×10 (uno por nivel, ver sección "Pendientes" — camino/tema por nivel) en vez de un único trazado; el ajuste de 14→10 filas (para que quepa completo en pantalla) se hizo antes, por un bug real reportado por el usuario (bottom bar tapando la base).
6. **Targeting "First":** interpretado como "el enemigo con mayor distancia recorrida a lo largo del camino" (`EnemyBase.get_progress()`), equivalente a "más cerca del final" dado que todos los enemigos comparten el mismo camino fijo.

---

## Mejoras Implementadas

- Core: Constants, EventBus, GameManager (máquina de estados: MENU/PLAYING/WAVE_INTERMISSION/PAUSED/GAME_OVER/GAME_WON), SaveManager (tutorial/sonido), MetaManager (propinas + 5 mejoras permanentes), AudioManager (stub funcional, reacciona a EventBus en vez de que cada feature le llame).
- Tablero: grilla 6×10, uno de 10 caminos/temas posibles por partida (fijo durante esa partida, ver "Pendientes" — camino/tema por nivel), cámara con paneo vertical (drag, hoy no-op), tap para construir/seleccionar.
- 3 torres (Salsa Verde, Hielo Horchata, Catapulta Guac) con upgrade in-run hasta nivel 3, venta al 70% de lo invertido.
- 3 enemigos (Básico, Rápido, Tank) con barra de vida visual, ralentización (Hielo Horchata) y daño en área (Catapulta Guac).
- 10 oleadas exactas del GDD, transición manual ("Iniciar Oleada") o auto-start a los 5s.
- Economía: oro por kill, propinas por oleada superada + bono de victoria.
- Metagame: tienda de 5 mejoras permanentes (daño/rango/cooldown/propinas/vida global) en `UpgradeScreen.tscn`.
- UI completa: MainMenu, Game (HUD con barra de compra, panel de selección, FTUE), PauseScreen, GameOverScreen, VictoryScreen, SettingsScreen (sonido on/off), UpgradeScreen.
- Ícono de la app (launcher): generado con IA en el mismo estilo "vector cartoon" que torres/enemigos (`tools/fetch_taco_app_icon.py`) — ver detalle en "Pendientes". Splash sigue procedural (`tools/gen_taco_icon.py::make_splash`, sin IA — ver `/gen-ai-art`).
- Tests GUT: 100 tests / 348 asserts, cubriendo toda la lógica pura (grid_math, upgrade_shop, wave_queue) y los autoloads/features con estado (GameManager, SaveManager, MetaManager, EnemyBase, TowerBase, Board, EnemySpawner, HUD).
- `tools/run_tests.sh` reforzado: además de respaldar/restaurar `save.json`/`meta.json`, ahora detecta si algún `test_*.gd` falló al PARSEAR (GUT lo excluye del conteo en vez de fallar la corrida — bug real descubierto en esta sesión) y fuerza exit code 1 en ese caso.
- `tools/probe_visual.gd`/`.tscn`: probe NO headless reutilizable para verificar el layout real con capturas PNG (regla CLAUDE.md #49). Uso: `godot --path . tools/probe_visual.tscn` (sin `--headless`); guarda `probe_main_menu.png`, `probe_game_initial.png` y `probe_game_with_towers.png` en `OS.get_user_data_dir()/probe_screenshots/`. Encontró y confirmó el arreglo de un bug real durante este build: el panel de FTUE sin `autowrap_mode` desbordaba el ancho de pantalla (texto y botón cortados) — ver `HUD.gd::_build_ftue_overlay()`.
- Build Android debug verificado localmente: `godot --headless --install-android-build-template --export-debug "Android" builds/debug/TacoDefender.apk` → BUILD SUCCESSFUL (APK ~89MB).

---

## Pendientes

- **Arte real:** RESUELTO. Fondos de pantalla completa (menú, detrás del tablero, tienda de mejoras) generados con Pollinations.ai vía `tools/fetch_taco_backgrounds.py`, integrados con `src/shared/background_style.gd` (velo oscuro para legibilidad de texto — ver `/gen-ai-art` Paso 7). Torres/enemigos generados con IA en estilo "vector cartoon" (`tools/fetch_taco_object_sprites.py`, flood-fill para remover fondo) para igualar la referencia de diseño; proyectiles siguen procedurales (`tools/gen_taco_sprites.py`, muy chicos — 12px — para que sobreviva algo de una imagen de IA). Icono y splash ya estaban resueltos proceduralmente desde el build inicial.
- **Camino/tablero durante la partida:** RESUELTO. Primer intento (fondo de pantalla completa semi-transparente detrás de la grilla) fue rechazado por el usuario con razón: una escena fija (callejón) no tiene ninguna relación visual con la FORMA real del camino en zigzag, así que quedaba confuso en vez de mejorar algo. Fix real: `tools/gen_board_tiles.py` genera 2 texturas tileables (`assets/sprites/board/{path,ground}_tile.png`, 120px = 2x el tile de 60px, con matemática de wraparound explícita para tileo perfecto — un generador de IA no garantiza esto, ver /gen-ai-art) y `Board.gd::_draw()` las dibuja con `draw_texture_rect()` celda por celda según `_path_cells` — la textura de piedra SOLO aparece donde el camino real pasa, así que el zigzag se ve como una calle de piedra real en vez de un fondo sin relación con el gameplay. Base/spawn quedan como tinte de color semi-transparente encima de la textura de piso (`COLOR_TILE_BASE_TINT`/`COLOR_TILE_SPAWN_TINT`). De paso se agregó resaltado de celdas construibles: con una torre pendiente de colocar (`Board._pending_tower_type != ""`), las celdas donde SÍ se puede construir se tiñen de verde translúcido (`Constants.COLOR_BUILD_HIGHLIGHT`) — patrón estándar de tower defense que el jugador pidió explícitamente. Encontrado en el camino: `_on_build_mode_requested()`/`_handle_tap()` mutaban `_pending_tower_type` sin llamar `queue_redraw()` — se agregó en ambos puntos. `game_bg.png` (callejón cenital, generado en el primer intento) se mantiene sin usar más que en el margen de ~15px del tablero — ambientación menor, ya no compite con la forma del camino.
- **UI in-game (HUD/paneles):** RESUELTO. `assets/theme/game_theme.tres` (generado por `tools/build_game_theme.gd`, referenciado en `project.godot` `[gui] theme/custom`) reestiliza TODOS los Button/Label/PanelContainer del juego (antes: tema plano default de Godot). Íconos agregados (`tools/gen_ui_icons.py`, procedural + `src/shared/icon_style.gd` como helper compartido de TextureRect): moneda de oro + corazón de vida en la barra superior, ícono de torre en el panel de selección y en los botones de compra (arte de `fetch_taco_object_sprites.py`), y símbolo de pausa/estrella de victoria/corazón roto de derrota en PauseScreen/VictoryScreen/GameOverScreen. Pendiente: animación de entrada/salida del toast (menor, cosmético).
- **Audio real:** RESUELTO. Los 9 `.wav` generados con `tools/gen_taco_audio.py` (síntesis pura Python, sin PIL/red — reutiliza los helpers de `gen_assets.py`). `AudioManager.gd` ya escuchaba EventBus para los 7 SFX de gameplay; se agregó loop manual de `music_loop.wav` vía `_music_player.finished` (el `.import` de un WAV nuevo no trae loop habilitado por default, y ese archivo está en `.gitignore` — depender de esa metadata habría sido frágil). `button_tap.wav` RESUELTO: `src/shared/button_sound.gd` (helper estático, `ButtonSoundGd.attach(button)`) conectado a los 32 botones de las 8 pantallas (incluye `LanguageSelectScreen`, agregada después). SFX de disparo por torre (muy frecuente) no se implementó — polish opcional.
- **CI/CD Google Play:** `.github/workflows/deploy-playstore.yml` ya tiene el package name real (`com.guacamolebit.tacodefender`), pero requiere secrets de GitHub (`ANDROID_KEYSTORE_BASE64`, `ANDROID_KEYSTORE_ALIAS`, `ANDROID_KEYSTORE_PASS`, `GOOGLE_PLAY_JSON`) que esta sesión no puede configurar. Ver `/android-deploy` antes de publicar — incluye la primera subida manual obligatoria a Play Console.
- **Multi-idioma:** RESUELTO (es default + en/pt_BR/fr) — ver sección Arquitectura arriba. Traducciones en `assets/translations/translations.txt` (50 keys) son un primer pase razonable, no revisadas por un hablante nativo de cada idioma — vale la pena una pasada de QA lingüística antes de publicar en mercados pt_BR/fr. De paso se encontró y arregló un bug de layout preexistente (no causado por i18n — los nombres en español ya eran igual de largos): `UpgradeScreen._build_row()` no tenía `autowrap_mode` en el label de nombre, y un nombre de mejora largo empujaba el botón "Comprar" fuera de la pantalla (mismo bug/mismo fix que el panel de FTUE de HUD.gd).
- **Feedback visual de "toast" (`EventBus.action_feedback`):** RESUELTO. Fade+slide de entrada (0.15s) y fade de salida (0.25s) vía `Tween` en `HUD.gd`, con un solo helper (`_play_toast_tween`) que mata cualquier tween en vuelo antes de arrancar uno nuevo (dos toasts seguidos no dejan animaciones peleando por la misma propiedad) y lo mata también en `_exit_tree()` (evita un `Tween` huérfano si la escena cambia a mitad de la animación).
- **Ícono de la app (launcher, el que se ve al instalar en el teléfono):** RESUELTO. El ícono anterior (`tools/gen_taco_icon.py`) era un taco de círculos planos sin relación con el estilo final del resto del arte del juego (torres/enemigos ya usan IA en estilo "vector cartoon", ver `/gen-ai-art`). Nuevo ícono generado con `tools/fetch_taco_app_icon.py`: un taco-mascota con un mini cañón de chile montado encima (la torre Salsa Verde, la más icónica/barata), en pose de defensa — une visualmente "Taco" + "Defender" en vez de un taco genérico. Mismo pipeline flood-fill que `fetch_taco_object_sprites.py`. Genera 4 archivos para cubrir Android adaptive icons (investigado con WebSearch, Godot sí soporta `launcher_icons/adaptive_background_432x432` aunque no estaba seteado antes):
  - `assets/icon.png` (512×512, fondo naranja marca sólido) — ícono plano/fallback, también `config/icon` de `project.godot`.
  - `assets/icon_adaptive_foreground.png` (432×432, transparente) — sujeto acotado al 58% del canvas (no 66%) porque la restricción real de la "safe zone" es sobre la DIAGONAL del bounding box (brazos extendidos a los costados), no el lado — un cuadrado de lado 66% ya se sale del círculo de máscara por las esquinas. Verificado recortando una máscara circular real sobre el compuesto antes de integrar (no alcanza con "se ve bien sin máscara").
  - `assets/icon_adaptive_background.png` (432×432, color sólido) — antes no existía ningún archivo de fondo adaptativo (`adaptive_background_432x432=""` vacío), Godot habría usado su fondo default.
  - `assets/icon_192.png` (192×192) — `launcher_icons/main_192x192`, launchers pre-adaptive-icon (Android <8).
  Verificado con un export debug real (`--export-debug "Android"`) que `android/build/res/mipmap-xxxhdpi-v4/icon_foreground.webp`/`icon_background.webp` reflejan el nuevo arte, no solo que los PNG fuente se vean bien. `splash.png` (pantalla de carga) NO se tocó — sigue siendo el taco procedural de círculos, pendiente de unificar si hace falta.
- **Un camino/tema distinto por nivel (variedad de partida a partida):** RESUELTO. El jugador reportó que, más allá del balance, el juego se sentía "siempre la misma partida con más enemigos". Se agregaron 10 `Constants.PATH_TEMPLATES` (zigzags geométricamente distintos: clásico, espejo, bandas altas, asimétrico, "peine", denso, mixto, "dorado"...) + 10 temas visuales propios (`tools/gen_board_tiles.py::THEMES` — piedra/desierto/nieve/selva/volcánico/playa/pantano/nocturno/otoño/dorado, `assets/sprites/board/{path,ground}_tile_N.png`). Decisión de diseño clave (confirmada explícitamente con el usuario antes de implementar, ver conversación): **una partida sigue siendo GDD al pie de la letra** — 10 oleadas, mismas torres/oro de principio a fin, tablero FIJO durante esa partida. Lo que cambia de partida a partida es cuál de los 10 templates se usa, decidido UNA sola vez en `Board._ready()` vía `GridMathGd.select_path_template_index(MetaManager.get_victories(), 10)` — el mismo contador de victorias que YA escalaba HP/recompensa de enemigos (`ENEMY_HP_BONUS_PER_VICTORY`). "Victorias" solo sube al completar las 10 oleadas, así que PERDER nunca cambia el tablero (el jugador reintenta el mismo nivel hasta ganarlo); recién entonces avanza. El índice cicla con `%` para que la variedad no se agote nunca, aunque el escalado de dificultad ya esté topado en victories=5. Como es un array fijo compilado en el juego (mismo patrón que `WAVE_DEFINITIONS`), el nivel N es IDÉNTICO en cualquier instalación — comparar "cómo superaste el nivel 3" entre teléfonos distintos no necesita servidor ni semilla. `Board.gd` expone `get_path_cells()` para que los tests no dependan de qué template esté activo (los tests de `test_board.gd` ya NO hardcodean `(1,1)` como celda construible — eso solo era válido para el template 0; ahora derivan la celda dinámicamente). `test_grid_math.gd` valida los 10 templates (in-bounds, sin celdas aisladas, tramos horizontales/verticales puros, fila nunca retrocede, empieza en fila 0 y termina en la última). Verificado visualmente con el probe (`tools/probe_visual.gd`, instrumentado temporalmente y revertido) forzando victories=0/2/9 — cada uno mostró forma Y paleta de color distintas, sin errores. Pendiente (no pedido en esta tanda): modificadores aleatorios por partida y una 4ta torre/enemigo nuevo — quedaron fuera de alcance a propósito, ver conversación.
- **Contador de vidas del HUD:** RESUELTO. El jugador reportó que un solo ícono de corazón + texto "3/3" no era claro ("tuve que jugar varias partidas para entender cómo funcionaba"). Reemplazado por una fila de N corazones (uno por vida máxima) en `HUD.gd::_rebuild_hp_hearts()` — corazón lleno (`heart.png`) o corazón roto (`broken_heart.png`, ya existía en `assets/sprites/ui/`, no hizo falta generar arte nuevo) según `current` vs índice. El máximo es fijo durante toda la partida (`GameManager._base_hp_max`), así que la fila solo se reconstruye cuando cambia el máximo, nunca en cada impacto — evita destruir/crear nodos innecesariamente. El tamaño de ícono se auto-ajusta hacia abajo para que hasta 8 corazones (con "Barra Blindada" nivel 5) sigan cabiendo en la misma caja de 154px donde antes vivía el ícono único.
- **Camino en zigzag / cámara (14→10 filas):** RESUELTO. El usuario reportó que la barra inferior parecía tapar parte del camino, y que el Game Over tardaba en aparecer tras dejar pasar un enemigo. Confirmado con matemática + probe real: el tablero (840px/14 filas) era 236px más alto que el hueco visible entre HUD y barra inferior (604px), y la cámara NUNCA hacía auto-scroll (`Board._camera.position` solo se muta en `_on_drag()`, confirmado instrumentando `tools/probe_visual.gd` temporalmente: `PROBE_CAMERA_Y start=332.0 end=332.0` tras 4s reales de enemigo caminando sin input de drag). En la posición de cámara inicial, las últimas 2 vueltas del zigzag Y LA BASE quedaban literalmente detrás de la barra inferior — el jugador veía el Game Over "tarde" porque el enemigo seguía caminando de verdad ahí, invisible. Fix: `Constants.GRID_ROWS` 14→10 (`PATH_TURN_CELLS` rediseñado, mismo estilo de zigzag de 6 columnas, 5 pasadas horizontales en vez de 7) — con 600px de tablero, `GridMathGd.compute_camera_bounds()` colapsa min/max al mismo punto, así que el tablero entero entra en pantalla sin scroll. El control de drag queda como no-op inofensivo (no se quitó código, el clamp ya generaliza ese caso). `test_grid_math.gd` tenía un test que afirmaba lo contrario (`test_compute_camera_bounds_gives_real_pan_range_for_this_board`) — se reescribió como `test_compute_camera_bounds_collapses_for_this_board`.
- **Ilusión de profundidad:** sigue sin aplicar — genuinamente no hace falta en un tower defense de grilla top-down 2D (camino fijo y visible, sin sensación de "acercarse a cámara" como un runner). No es un pendiente, es una decisión de diseño ya tomada (ver sección Arquitectura).
- **VFX de impacto:** RESUELTO. `src/features/vfx/impact_vfx.gd` (helper estático, `CPUParticles2D` de un solo uso con auto-limpieza vía su propia señal `finished`) + hit-flash (`modulate` sobre-expuesto vía `Tween`) en `EnemyBase.gd` para golpes no letales. Cada torre tiene su color de impacto (verde Salsa Verde, celeste Hielo Horchata, marrón Catapulta Guac — mismos tonos que los proyectiles); la muerte de un enemigo agrega un burst de polvo neutro. **Nota de test:** un test que mata un enemigo deja el burst de partículas vivo por 0.35s reales después de que el test ya terminó, y GUT reporta "test script still has children when all tests finished" al cerrar la suite — no es un leak real (ver comentario en `EnemyBase._die()`), mismo espíritu que la regla CLAUDE.md #55 (loop de música).
- **Balance real:** revisado con investigación competitiva (Bloons TD/Kingdom Rush/Plants vs Zombies mobile). Hallazgo original: Salsa Verde (15 dmg, $50, cd 1.2s) mata de un golpe a Básico (10 HP) y Rápido (5 HP), y su cooldown es más corto que el intervalo de spawn de la Oleada 1 — una sola torre limpia esa oleada sin recibir daño. En ese momento esos valores eran literales del GDD y no se tocaron. Hallazgo más grande de esa pasada: no existía NINGÚN escalado de dificultad entre partidas — `MetaManager.get_victories()`/`get_best_wave()` solo se leían para texto en MainMenu, nunca afectaban gameplay, mientras las 5 mejoras permanentes solo buffean al jugador. Se agregó escalado de dificultad por repetición (`Constants.ENEMY_HP_BONUS_PER_VICTORY`/`ENEMY_REWARD_BONUS_PER_VICTORY`, +15% HP y +15% recompensa de oro por victoria previa, tope de 5) en `EnemyBase._ready()`. Tests de `test_enemy_base.gd` derivan el HP/recompensa esperado con la misma fórmula (relativo al `MetaManager` real). De paso se adaptó `.claude/.agents/game-designer.md` (tenía el checklist del survivor-shooter genérico del template — power-ups/XP/jefes que no existen acá — con un checklist propio de tower defense).
  **Actualización (sesión posterior):** el jugador reportó directamente "el juego es demasiado fácil, fácilmente se puede derrotar a todos los enemigos en las dos primeras filas" — confirmando en la práctica el hallazgo original de "one-shot" que antes se había dejado sin tocar por apego literal al GDD. Esta vez se corrigió la causa raíz: HP de enemigos subido en `Constants.gd` (Básico 10→35, Rápido 5→18, Tank 100→220), calculado contra el tiempo real de tránsito por rango de una torre (2×rango/velocidad) para que ni siquiera una sola Torre Salsa Verde a máximo cooldown alcance a matar a un Básico o un Rápido de un solo impacto — ahora hacen falta 2-3. Las recompensas de oro NO cambiaron (mismo oro total por oleada, solo tarda más en limpiarse). `taco-defender-gdd.md` (tabla de enemigos) se actualizó junto con una nota explícita de la desviación — criterio de la regla CLAUDE.md #63 ("el GDD es punto de partida, no ley inmutable", aplicado ahora que SÍ hay un pedido explícito de balance de por medio, no solo un hallazgo unilateral).
  **Verificación de balance por nivel (10 templates de camino):** a pedido explícito ("confirma que los niveles no se conviertan en imposibles"), análisis matemático de las 10 celdas construibles por template (`Constants.PATH_TEMPLATES`) contra el DPS necesario para limpiar la Oleada 10 con el escalado de dificultad al tope (victories≥5, +75% HP/recompensa, `ENEMY_VICTORY_SCALING_CAP`). El template más ajustado es el 7 ("denso", tema nocturno): solo 15 celdas construibles (25% del tablero, contra 25-37 celdas en el resto). HP total de la Oleada 10 a +75%: Tank 385×6 + Rápido 31.5×15 + Básico 61.25×10 ≈ 3395 HP. Con 15 torres Salsa Verde nivel 3 (25 dmg / 1.2s = 20.8 DPS c/u, sin contar Catapulta Guac ni el efecto de ralentización de Hielo Horchata, que solo mejorarían este número), el "tiempo de foco" necesario para limpiar esa oleada es ≈3395/(15×20.8)≈10.9s, contra una ventana de spawn de la oleada de ≈15.5s (sin contar el tiempo de tránsito adicional del último enemigo) — margen cómodo (~30%) incluso en el escenario más conservador posible (un solo tipo de torre, sin AoE, sin slow). **No es una simulación completa** (no corrió una partida real oleada por oleada) — es un análisis analítico de orden de magnitud, mismo criterio de honestidad que el resto de los hallazgos de balance de este documento: no se afirma más certeza de la que da la matemática. Se agregó `test_every_template_has_enough_buildable_cells()` en `test_grid_math.gd` como red de seguridad (piso de 12 celdas construibles) para que un futuro template nuevo no caiga por debajo de este margen sin que nadie lo note — no porque el piso actual (15) esté en riesgo, sino para blindar templates futuros.

---

## Valores de Balance (GDD v1.1 + ajuste de balance post-playtesting, ver "Pendientes")

### Economía
Oro inicial: $100 · Venta de torres: 70% de lo invertido · Auto-start de oleada: 5s

### Enemigos
| Tipo | HP | Velocidad | Recompensa |
|---|---|---|---|
| Básico (mosca) | 35 | 80 px/s | $5 |
| Rápido (cucaracha) | 18 | 200 px/s | $8 |
| Tank (ratón) | 220 | 30 px/s | $25 |

### Torres
| Torre | Costo | Daño | Rango | Cooldown | Upgrade |
|---|---|---|---|---|---|
| Salsa Verde | $50 | 15 | 150px | 1.2s | +$35: +5 dmg, +15 rango |
| Hielo Horchata | $75 | 8 | 120px | 1.5s | Slow 50%/2s base; +$50: +25% duración/nivel |
| Catapulta Guac | $120 | 25 (AoE R=50px) | 200px | 2.5s | +$80: +10 dmg AoE |

### Oleadas (10 — ver `Constants.WAVE_DEFINITIONS`)
1. 5 Básico (1.5s) · 2. 8 Básico (1.2s) · 3. 5 Básico+3 Rápido (1.0s) · 4. 10 Rápido (0.6s) · 5. 6 Básico+1 Tank (1.0s) · 6. 12 Rápido+2 Tank (0.8s) · 7. 15 Básico+8 Rápido (0.5s) · 8. 4 Tank+10 Rápido (1.0s) · 9. 20 Básico+15 Rápido (0.4s) · 10. 6 Tank+15 Rápido+10 Básico (0.5s)

### Metagame
Propina por oleada: 10 · Bono victoria: 50 · 5 mejoras, 5 niveles c/u, costo 100/250/500/1000/2000 propinas.
